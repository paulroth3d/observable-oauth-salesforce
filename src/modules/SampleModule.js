/**
 * Simple module that adds two numbers together
 * @param {number} a -
 * @param {number} b -
 * @returns {number} - a+b
 */
const sum = (a, b) => {
  return a + b;
};

module.exports = ({
  sum
});
